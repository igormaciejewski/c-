//Z.1
Random r = new Random();
int[] T = new int[100];
for (int i = 0; i < T.Length; i++)
{
    T[i] = r.Next(100,1000);
}
for (int i = 0; i < T.Length; i++)
{
    Console.WriteLine(T[i] + " ");
}
int suma = 0;
for (int i = 0; i < T.Length; i++)
{
    suma = suma + T[i];

}
Console.WriteLine(suma);
Console.WriteLine(suma / 100);
