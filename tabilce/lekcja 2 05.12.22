//Random 
Random r = new Random();
Console.WriteLine(r.Next(10, 20));

Random r = new Random();
int[] T = new int[20];
for (int i = 0; i < T.Length; i++)
{
    T[i] = r.Next(1,200);
}
for (int i = 0; i < T.Length; i++)
{
    Console.WriteLine(T[i] + " ");
}


//Z.1

